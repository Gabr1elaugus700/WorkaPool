= Sql.Database("192.168.0.2", "sapiens", [Query="SELECT #(lf)    nfv.datemi AS [EMISSAO],#(lf)    CASE #(lf)        WHEN ipv.codfil = 1 THEN 'MGA' #(lf)        ELSE #(lf)            CASE #(lf)                WHEN ipv.codfil = 2 THEN 'ITJ' #(lf)                ELSE 'CTB' #(lf)            END #(lf)    END AS [FIL],#(lf)    cli.nomcli AS [CLIENTE],#(lf)    ram.desram AS [SEG],#(lf)    nfv.numnfv AS [NUM_NFV],#(lf)    cli.sigufs AS [ESTADO],#(lf)    ipv.codpro AS [COD_PRO],#(lf)    ipv.codder AS [COD_DEV],#(lf)    ipv.cplipv AS [PRODUTO],#(lf)    nfv.qtdemb AS [VOLUME],#(lf)    ipv.codfam AS [FAMILIA],#(lf)    ipv.qtdfat AS [QUANT],#(lf)    ipv.preuni AS [VLR UNIT],#(lf)    ipv.preuni * ipv.qtdfat AS [TOTAL],#(lf)    ipd.usu_pretab AS [CUSTO],#(lf)  #(tab)ipv.tnspro AS [CFOP],#(lf)    ipv.vlripi AS [IPI],#(lf)    ipv.vlricm AS [ICMS],#(lf)    ipv.vlrpif AS [PIS],#(lf)    ipv.vlrcff AS [COFINS],#(lf)    SUM(ped.usu_frekgs * ipv.qtdfat) AS [FRETE INC], -- Frete Incluso (no preço do produto)#(lf)    SUM(ped.usu_freuni * ipv.qtdfat) AS [FRETE DEST], -- Frete destacado #(lf)    rep.aperep AS [VENDEDOR] #(lf)    ISNULL(grp.desgrp,'OUTROS PRODUTOS') AS [PRODUTONOME] #(lf)FROM e140ipv ipv#(lf)LEFT JOIN e140nfv nfv #(lf)    ON nfv.numnfv = ipv.numnfv #(lf)    AND nfv.codemp = ipv.codemp     #(lf)    AND nfv.codfil = ipv.codfil #(lf)    AND nfv.codsnf = ipv.codsnf#(lf)LEFT JOIN e120ipd ipd #(lf)    ON ipd.codemp = ipv.codemp #(lf)    AND ipd.codfil = ipv.codfil #(lf)    AND ipd.numped = ipv.numped #(lf)    AND ipd.seqipd = ipv.seqipd#(lf)LEFT JOIN e120ped ped #(lf)    ON ped.codemp = ipd.codemp #(lf)    AND ped.codfil = ipd.codfil #(lf)    AND ped.numped = ipd.numped#(lf)LEFT JOIN e090rep rep #(lf)    ON rep.codrep = ped.codven#(lf)LEFT JOIN e001tns tns #(lf)    ON tns.codemp = ipv.codemp #(lf)    AND tns.codtns = ipv.tnspro#(lf)LEFT JOIN e085cli cli #(lf)    ON cli.codcli = nfv.codcli#(lf)LEFT JOIN e026ram ram #(lf)    ON ram.codram = cli.codram#(lf)WHERE #(lf)    nfv.datemi > '01/04/2025' #(lf)    AND ped.numped > 0#(lf)    AND nfv.sitnfv = 2 #(lf)    AND tns.venfat = 'S'#(lf)    AND ipv.qtdfat > ipv.qtddev#(lf)GROUP BY #(lf)    nfv.datemi,#(lf)    ipv.codfil,#(lf)    cli.nomcli,#(lf)    nfv.numnfv,#(lf)    ram.desram,#(lf)    cli.sigufs,#(lf)    ipv.codpro,#(lf)    ipv.codder,#(lf)    ipv.tnspro,#(lf)    ipv.cplipv,#(lf)    nfv.qtdemb,#(lf)    ipv.codfam,#(lf)    ipv.qtdfat,#(lf)    ipv.preuni,#(lf)    ipd.usu_pretab,#(lf)    ipv.vlripi,#(lf)    ipv.vlricm,#(lf)    ipv.vlrpif,#(lf)    ipv.vlrcff,#(lf)    ipd.vlrfre,#(lf)    rep.aperep", CommandTimeout=#duration(0, 0, 10, 0)])